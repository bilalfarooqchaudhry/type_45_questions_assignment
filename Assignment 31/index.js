"use strict";
/*let users=['asad','usman','bilal','rafia','hassan'];
for(let i=0 ; i<5;i++){

  let newuser=users.pop();
  console.log("🚀 ~ file: index.ts:5 ~ newuser:", newuser)
  console.log(users)
  }
  if (users.length = 0){

    console.log('we need to find some users');
  }  */
let users = [];
if (users.length > 0) {
    users = [];
    console.log("All usernames have been removed.");
}
else {
    console.log("We need to find some users!");
}
